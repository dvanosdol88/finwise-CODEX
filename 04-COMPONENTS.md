# Step 4 — Components

## Files added
- `src/components/CostAnalysisCalculator.tsx` — your calculator + controls + Recharts chart + Share + deep links
- `src/components/charts/*` — proof charts used on `/save`
- `src/components/save/SaveProofClient.tsx` — deeper proof visuals page (client component)

## Landing page sections
This bundle does NOT force the Upgrade/Improve/Save section copy; keep them as simple scroll sections below the calculator.
