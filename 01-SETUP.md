# Step 1 — Install dependencies and confirm baseline

## 1) Install chart dependencies
```bash
npm install recharts react-is
```

## 2) Confirm Tailwind is working
Run:
```bash
npm run dev
```

You should see the existing template page. Then proceed to Step 2 to add Inter + tokens and Step 3+ to add the calculator + pages.
